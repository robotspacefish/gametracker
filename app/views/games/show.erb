<main>
  <div>
    <h2><%= @game.title %></h2>
    <% if @game.cover_art %>
      <img src="<%= @game.cover_art.url %>" alt="<%= @game.title%> cover art">
    <% end %>

    <%= erb :'partials/_add_game_by_platform_choice_to_user_form', locals: {game: @game} %>

    <% if @game.summary %>
      <p><%= @game.summary %></p>
    <% end %>

  </div>

</main>