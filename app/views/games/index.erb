<h1>Games</h1>

<main>
  <div class="games">
  <h2>Add a Game to Your Library</h2>
    <% @games.each do |game| %>
      <div class="game">
      <!-- TODO  add game form -->
        <%= game.title %>
      </div>
    <% end %>
  </div>
</main>